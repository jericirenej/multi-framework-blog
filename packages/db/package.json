{
  "name": "@/db",
  "module": "index.ts",
  "type": "module",
  "exports": {
    "./*": "./src/*"
  },
  "scripts": {
    "lint": "eslint .",
    "seed": "bunx --bun kysely seed run",
    "migrate:rollback": "bunx --bun kysely migrate:rollback --all",
    "migrate": "bunx --bun kysely migrate:latest",
    "postmigrate": "bun run ./src/helpers/infer-types-script.ts",
    "postmigrate:rollback": "bun run postmigrate"
  },
  "private": true,
  "devDependencies": {
    "@types/bun": "catalog:",
    "typescript-eslint": "catalog:",
    "@eslint/js": "catalog:",
    "eslint": "catalog:",
    "kysely-codegen": "^0.18.5",
    "kysely-ctl": "^0.13.1"
  },
  "dependencies": {
    "@/env": "workspace:*",
    "typescript": "catalog:",
    "kysely": "^0.28.2",
    "kysely-bun-sqlite": "^0.4.0"
  }
}

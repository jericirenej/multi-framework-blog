{
  "name": "@/db",
  "type": "module",
  "exports": {
    "./*": "./src/*.ts"
  },
  "scripts": {
    "lint": "eslint",
    "tsc": "tsgo --noEmit --project tsconfig.json",
    "check": "pnpm tsc && pnpm lint",
    "seed": "kysely seed run",
    "migrate:rollback": "kysely migrate:rollback --all",
    "migrate": "kysely migrate:latest",
    "postmigrate": "tsx ./src/helpers/infer-types-script.ts",
    "postmigrate:rollback": "bun run postmigrate"
  },
  "private": true,
  "devDependencies": {
    "@eslint/js": "catalog:",
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "catalog:",
    "@typescript/native-preview": "catalog:",
    "date-fns": "catalog:",
    "eslint": "catalog:",
    "kysely-codegen": "^0.18.5",
    "typescript": "catalog:",
    "kysely-ctl": "^0.13.1",
    "typescript-eslint": "catalog:",
    "zx": "catalog:"
  },
  "dependencies": {
    "@/env": "workspace:",
    "argon2": "catalog:",
    "better-sqlite3": "^12.1.1",
    "dotenv": "catalog:",
    "kysely": "catalog:",
    "kysely-bun-sqlite": "^0.4.0",
    "uuid": "catalog:"
  }
}

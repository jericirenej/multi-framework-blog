import tw from "../../private";

const box = tw`flex min-w-[50ch] flex-col items-center gap-5 text-neutral-700`;
const paragraphsWrapper = tw`relative flex w-full max-w-prose flex-col gap-5 text-left text-black`;

export default { box, paragraphsWrapper };
